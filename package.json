{
  "name": "kaveep-core",
  "version": "0.1.0",
  "description": "The orchestration runtime foundation of the KAVEEP ecosystem.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run build && npm test",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\""
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
